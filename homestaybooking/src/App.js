import React from 'react';
import SearchBar from './components/SearchBar/SearchBar';

function App() {
  return React.createElement('div', { className: 'App' }, React.createElement(SearchBar, null));
}

export default App;
